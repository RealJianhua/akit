android代码小工具

待完善：
db -> greendao:
1. 优点:
1.1 注解的方式ORM映射，方便；
1.2 通过开发工具，读取注解，生成DAO代码，而不是运行时反射运行，性能没有损耗。我长时间不愿意做ORM，就是因为性能，生成代码这种方式没想到过。
1.3 有缓存

2. 缺点：
2.1 暂时发现一点，不喜欢在移动项目里做一对一或多对多的数据库映射。更希望冗余一点，把对象属性序列化到一条记录里。因此会修缮一下。